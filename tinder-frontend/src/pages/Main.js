import React from 'react';
import { View } from 'react-native';
import SwipeDeck from '../organisms/SwipeDeck';

const Main = () => (
  <View style={{ flex: 1 }}>
    <SwipeDeck />
  </View>
);

export default Main;